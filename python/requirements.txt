# Pinned to major versions known to interoperate as of 2026-07. Minor updates
# should be safe; a `pip install -U` will pull the latest patch.
gymnasium>=1.0,<2.0
stable-baselines3>=2.4,<3.0
pymunk>=6.9,<8.0
# pygame-ce is a maintained fork; upstream pygame 2.6.1 has a circular-import bug on Python 3.14.
pygame-ce>=2.5,<3.0
imageio>=2.35,<3.0
numpy>=1.26,<3.0
torch>=2.4,<3.0
tensorboard>=2.16,<3.0
tqdm>=4.66,<5.0
